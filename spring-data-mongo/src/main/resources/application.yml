spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: javaalien
  application:
    name: spring-data-mongo

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG


